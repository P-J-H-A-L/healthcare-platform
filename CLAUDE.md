# my-project — Claude Code 지침

## 코드 리뷰 워크플로우

PR이 열리거나 커밋이 푸시되면 세 전문 에이전트가 **병렬로** 독립 worktree에서 코드를 분석하고, 오케스트레이터가 결과를 취합해 GitHub PR에 코멘트를 작성한다.

### 에이전트 구성

| 에이전트 | 역할 | 파일 |
|----------|------|------|
| security-reviewer | SQL Injection, XSS, 시크릿 노출, 명령어 인젝션 탐지 | `.claude/agents/security-reviewer.md` |
| performance-reviewer | N+1 쿼리, 불필요한 루프, 메모리 누수 탐지 | `.claude/agents/performance-reviewer.md` |
| style-reviewer | 네이밍 컨벤션, 코드 복잡도, 중복 코드 탐지 | `.claude/agents/style-reviewer.md` |

### 실행 방식

- 각 에이전트는 `isolation: worktree`로 독립된 git worktree에서 실행된다.
- 세 에이전트는 병렬(parallel)로 동시에 실행되어 전체 리뷰 시간을 단축한다.
- 오케스트레이터는 결과를 심각도 순(HIGH → MEDIUM → LOW)으로 병합한다.

### 결과 파일 저장 규칙

각 에이전트는 분석 완료 후 결과를 아래 경로에 마크다운 파일로 저장한다.

| 에이전트 | 저장 경로 |
|----------|-----------|
| security-reviewer | `/tmp/review/security.md` |
| performance-reviewer | `/tmp/review/performance.md` |
| style-reviewer | `/tmp/review/style.md` |

오케스트레이터는 세 파일을 모두 읽은 뒤 심각도 순(HIGH → MEDIUM → LOW)으로 병합하여 최종 리뷰를 `/tmp/review/final.md`에 작성한다.

```
/tmp/review/
├── security.md      ← security-reviewer 출력
├── performance.md   ← performance-reviewer 출력
├── style.md         ← style-reviewer 출력
└── final.md         ← 오케스트레이터 최종 병합본
```

**저장 시점:** 각 에이전트는 분석이 끝나는 즉시 파일을 저장한다. 오케스트레이터는 세 파일이 모두 존재할 때만 `final.md`를 생성한다.

**파일 형식:** 각 결과 파일은 에이전트별 분석 요약으로 시작하고, `final.md`는 세 파일의 이슈를 심각도 기준으로 통합 정렬한 뒤 전체 요약 테이블을 포함한다.

### 결과 취합 기준

오케스트레이터가 `final.md`를 생성할 때 아래 세 단계를 순서대로 적용한다.

#### 1. 중복 이슈 제거

동일 파일·라인을 두 개 이상의 에이전트가 지적한 경우, **심각도가 가장 높은 이슈 하나만 유지**하고 나머지는 제거한다.

- 심각도가 같으면 보안 > 성능 > 스타일 순으로 우선한다.
- 제거된 이슈는 해당 항목의 `참고` 필드에 출처 에이전트를 명시한다.

```
[높음] frontend/index.html:828 - 솔트 없는 SHA-256 비밀번호 해싱   ← 유지
  참고: performance-reviewer도 동일 라인 지적 (중간 심각도) → 병합 제거
```

#### 2. 우선순위 매트릭스

중복 제거 후 남은 이슈 전체에 대해 **수정 난이도 × 영향도** 매트릭스를 생성한다.

수정 난이도 기준:
- **쉬움**: 상수 추출, 변수명 변경, 단순 함수 분리 등 1~2줄 수준 수정
- **중간**: 로직 재구성, 헬퍼 함수 도입, 자료구조 교체 등 10~30줄 수준 수정
- **어려움**: 아키텍처 변경, 외부 라이브러리 교체, 암호화 도입 등 설계 변경 필요

`final.md`에 아래 형식의 매트릭스 테이블을 포함한다.

```
## 우선순위 매트릭스

| 이슈 요약 | 심각도 | 수정 난이도 | 영향도 | 우선순위 |
|-----------|--------|------------|--------|---------|
| SHA-256 솔트 없음 | HIGH | 어려움 | 높음 | P1 |
| CSS Injection | HIGH | 중간 | 높음 | P1 |
| Math.random() ID | MEDIUM | 쉬움 | 중간 | P2 |
| ...           | ...  | ...  | ...  | ...  |
```

우선순위 산정 규칙:
- **P1**: 심각도 HIGH 또는 (심각도 MEDIUM + 영향도 높음)
- **P2**: 심각도 MEDIUM + 영향도 중간, 또는 (심각도 LOW + 영향도 높음 + 수정 쉬움)
- **P3**: 그 외 낮은 심각도·낮은 영향도 이슈

#### 3. 높은 심각도 요약 섹션

`final.md` 최상단에 HIGH 이슈만 별도 추출한 요약 섹션을 추가한다.

```markdown
## 🚨 즉시 조치 필요 (HIGH)

> PR 머지 전 반드시 수정해야 하는 항목입니다.

| # | 파일:라인 | 이슈 | 에이전트 | 수정 난이도 |
|---|-----------|------|----------|------------|
| 1 | index.html:828 | 솔트 없는 SHA-256 해싱 | security | 어려움 |
| 2 | index.html:1428 | CSS Injection | security | 중간 |
| ...

**총 N건 — 머지 차단 권고**
```

이 섹션은 GitHub PR 코멘트의 첫 블록으로도 그대로 사용된다.

### 심각도 기준

- **높음(HIGH)**: 즉시 수정 필요. PR 머지 차단 권고.
- **중간(MEDIUM)**: 머지 전 수정 권고.
- **낮음(LOW)**: 선택적 개선 사항. 머지 허용.

### PR 코멘트 형식

```
## 코드 리뷰 결과

### [높음] 파일명:라인번호 — 취약점/이슈 제목
- 원인: ...
- 권고: ...

---
보안: N건 | 성능: N건 | 스타일: N건 | 합계: N건
```

## 일반 지침

- 코드 수정 시 기존 테스트를 깨뜨리지 않는다.
- 새 기능에는 반드시 테스트를 추가한다.
- 비밀값(API 키, 비밀번호)은 절대 소스코드에 하드코딩하지 않는다. 환경변수 또는 시크릿 매니저를 사용한다.
- 커밋 메시지는 `type(scope): 설명` 형식을 따른다.
